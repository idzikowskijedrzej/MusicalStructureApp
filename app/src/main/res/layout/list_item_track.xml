<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/list_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/albumCover"
        android:layout_width="@dimen/albumCoverDimen"
        android:layout_height="@dimen/albumCoverDimen"
        android:layout_alignParentLeft="true"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:padding="@dimen/albumCoverPadding"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_toRightOf="@id/albumCover"
        android:gravity="left"
        android:orientation="vertical">

        <TextView
            android:id="@+id/trackName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingLeft="@dimen/listItemPadding"
            android:paddingRight="@dimen/listItemPadding"
            android:paddingTop="@dimen/listItemPadding"
            android:text="Livin on the edge"
            android:textSize="@dimen/topTextDimen" />

        <TextView
            android:id="@+id/artistName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/listItemPadding"
            android:paddingLeft="@dimen/listItemPadding"
            android:paddingRight="@dimen/listItemPadding"
            android:text="Aerosmith"
            android:textColor="@color/colorPrimaryDark"
            android:textSize="@dimen/topArtistDimen"
            android:textStyle="italic" />
    </LinearLayout>

    <ImageView
        android:layout_width="@dimen/playButton"
        android:layout_height="@dimen/playButton"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_margin="@dimen/helperPadding"
        android:src="@drawable/play"
        android:visibility="invisible" />

</RelativeLayout>